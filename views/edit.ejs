<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit your post</title>
  </head>
  <body>
    <h1>Edit your post</h1>
    <div>
        <p>Username : <%= post.username %></p>
        <p> ID : <%= post.id %> </p>
        <form action="/instagram/<%= post.id %>?_method=PATCH"  method="post">
            <textarea name="content" cols="35" rows="20"><%= post.content %></textarea>
            <br>
            <button>Submit</button>
        </form>
    </div>
    </div>
  </body>
</html>
